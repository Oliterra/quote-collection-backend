spring:
  datasource:
    url: jdbc:postgresql://quotes-collection-db:5432/quotes-collection
    username: nadezhda
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate.ddl-auto: none
    database-platform: org.hibernate.dialect.PostgresPlusDialect
    show-sql: true
server:
  port: 8080
